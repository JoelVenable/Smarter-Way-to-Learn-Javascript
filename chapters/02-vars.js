var name = "Mark";

var bully = "Mark";


console.log(name);  // Mark
name = "Mike";

console.log(name);  // Mike


var nationality;   // variable is declared, not assigned.

nationality = "U.S.";
//  JS doesn't care what the variable is called (as long as reserved words aren't used).

// Vars cannot start with numbers.

// Meaningful names help with code readability:

var lastName = "Smith";
var cityOfOrigin = "New Orleans";
var aussieGreeting = "g'Day";

//  Spaces between equals sign is a style choice, not a requirement.

var thanx = "Thanks for your input!";
console.log(thanx);  // Thanks for your input!

